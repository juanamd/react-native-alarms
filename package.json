{
  "name": "react-native-alarms",
  "version": "1.5.0",
  "description": "React Native library for interacting with Android alarms.",
  "keywords": [
    "react-native",
    "alarms",
    "alarmmanager",
    "android"
  ],
  "author": "ioddly",
  "license": "MIT",
  "main": "lib/alarms.js",
  "types": "types/alarms.d.ts",
  "scripts": {
    "build": "babel src/ -d lib/ && flow-copy-source src lib",
    "prepublish": "yarn run build"
  },
  "peerDependencies": {
    "react-native": ">=0.44"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-flow": "^7.0.0",
    "flow-bin": "^0.144.0",
    "flow-copy-source": "^2.0.7"
  }
}
